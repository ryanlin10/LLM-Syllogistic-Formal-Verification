# Configuration file for SyLLM project

# Model Configuration
model:
  # Base model (used for training and as base for inference with LoRA)
  base_model: "mistralai/Mistral-Small-3.2-24B-Instruct-2506"
  # LoRA adapter path for inference (optional - if not set, uses base_model only)
  lora_adapter: "/Users/ryanlin/Desktop/SyLLM/mistralai_Mistral_Small_3.2_24B_Instruct_2506_20260120_021836/final"

# Data Configuration
data:
  train_path: "./data/train.jsonl"
  val_path: "./data/val.jsonl"
  test_path: "./data/test.jsonl"
  seed: 42

# Paths
paths:
  data_dir: "./data"
  models_dir: "./models"
  logs_dir: "./logs"
  outputs_dir: "./outputs"

